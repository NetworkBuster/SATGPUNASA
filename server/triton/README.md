Triton & ONNX helper (placeholder)

- `convert_to_onnx.py`: script to convert PyTorch models to ONNX
- `triton_config/`: example model config for Triton Inference Server

Note: This is a starting point; actual conversion requires model file path and testing on a GPU node.